# V0.8.7 使用说明

## 显示缩放与响应图

软件使用 Qt 的 DPI 感知布局。在 Windows 100% 和 150% 缩放下，标注和坐标轴标题均使用相同的 10 pt 正常字重，系统负责转换显示像素，不再对标注额外放大。窗口较窄时工具栏自动换行；高度不足时，可滚动查看完整的三个同步响应图。

响应图标注使用透明背景。电流图的触发时刻、载荷图的触发及响应时刻，均以圆点标记竖虚线与实测曲线的插值交点。文字水平放在交点上方或下方，视图缩放或尺寸变化后重新排布。虚线在文字处留出间隙，实测曲线保持连续。

I₁₀%、I₁₀₀%、F₁%、F₆₃%、F₉₀%、F₁₀₀% 保留工程下标。t₁%、t₆₃%、t₉₀% 直接显示 `= xx ms`。使用默认 10% 电流触发设置时，响应时间为对应力阈值交点时刻减去 I₁₀% 电流交点时刻。改变触发比例会改变计时零点；导出表保留实际触发比例和时间。

## 电流—阻尼力与分组迟滞图

1. 从公共数据源打开包含完整循环和电流平台的数据，选择 BMW 或 Audi 评价规则。
2. 设置速度分组容差，默认 3%。程序根据中心行程实测速度分组，容差表示相对组内最低速度的最大偏差。速度点相近时，应减小容差；分组值会写入导出设置。
3. 点击“分析迟滞”。每个速度组独立进行原有 BMW 升/降电流配对或 Audi KFM 前后平台计算，不跨速度求均值。
4. 首图“全部速度：电流—阻尼力”叠加各速度曲线。纵轴统一为“压缩<--阻尼力(N)-->复原”，复原显示正阻尼幅值、压缩显示负阻尼幅值。原始数据及导出数值保留其原始符号。
5. 图形选择器自动生成各速度的电流—阻尼力迟滞图，以及各电流的速度—阻尼力图。BMW 实线表示升电流过程，虚线表示降电流过程；Audi 按实测平台先后连接，保留 KFM 往返路径。
6. “结果数据”页保留汇总和工况明细；Excel 导出包含所有分组图，PNG 导出当前选中图。

要比较不同速度，点击“加载多速度迟滞数据…”并一次选择各速度的原始文件。程序会隔离每个文件的 Block ID，再按中心行程实测速度统一分组；文件选择框只选中一个文件时，图中只能出现一个实测速度。导出前程序会自动切换到迟滞图形页并重算纵轴范围，保证压缩方向完整显示。

只有输入数据中真实存在的速度、电流与方向才会生成曲线。未配对工况可显示实测点，但不会补造迟滞值；单一电流只有一个速度时，其速度图只有实测点。响应试验中的电流跳变数据不等于完整的电流扫描迟滞试验。

## 其它功能

- 每次导入新数据后，【图形】默认以第 1 个可绘制的原始数据列为 X 轴，并选择其余原始数据列作为 Y 轴。Block ID、Source Row 和分析派生列仍可手动选择，但不加入默认组合；重新分析同一文件时保留手动选择。
- 目标速度与客户规则独立，可输入 0.1、0.3、0.6、1.0 m/s 等正有限速度点。
- 气体反弹力支持加上或减去，原始载荷不覆盖。
- t₉₀% 和迟滞限值未设置时，不自动判定客户合格与否。
